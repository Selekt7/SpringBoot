server:
  port: 9090
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ingredienti
    username: root
    password: Modificapass98!

  jpa:
    generate-ddl: true
    hibernate:
    ddl-auto: update

logging:
  level:
    org.hibernate.SQL: DEBUG
  org.hibernate.type.descriptor.sql.BasicBinder: TRACE
